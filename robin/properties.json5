{
  // Send RSET command before additional envelopes.
  rsetBetweenEnvelopes: false,
  // Logging config.
  logging: {
    data: false,
    // Log email DATA sent and received.
    textPartBody: false
    // Log MIME build case text/* type part contents.
  }
}